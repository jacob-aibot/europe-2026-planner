# Waypoint — Phase 1 needs no secrets, no API keys and no network.
# This file exists so later phases add config here and never in the repo.

# Phase 1 (optional): where the dev static server listens.
WAYPOINT_PORT=5173

# Phase 2+ (not used yet):
# DATABASE_URL=
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# MAIL_OAUTH_CLIENT_ID=
# MAIL_OAUTH_CLIENT_SECRET=
